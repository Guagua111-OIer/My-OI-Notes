2024/10/13
P10491 [USACO09NOV] The Chivalrous Cow B
这道题其实思路不难想，也一遍 AC 了，不过敲代码时还是有一点手生，各种编译错误，所以千万不要在代码里面放认为很黑科技的东西（比如 operator），因为那样调试、调错都有害处。
就是一个简单的 BFS 模板，牛的动作有 8 个方向，x 和 y 的变化自己模拟就好，返回答案时要注意考虑不能从起点走到终点的情况。其实测试点中并没有这种情况，但是由于我的 bfs 是直接返回答案的，所以随便返回个值，比如 -1。开一个 vis 数组记录每个点走到过没有，不开牛可能会出现在两点之间反复横跳的现象。整体还是比较简单（毕竟一遍 AC），差不多一个 T2 的难度。

---

2024/10/13
P3958 [NOIP2017 提高组] 奶酪
真不愧是 S 组的 T1，一道活生生的黄题（普及/提高-），调了半天，接近 1h，要是放 J 组，个人感觉能放 T3。
首先想到了 DFS。思路很简单：每次遍历每个洞，看能不能入洞，能就递归这个洞，最后如果到了上表面就 return 然后输出 "Yes"。

结果：

WA WA WA AC AC TLE TLE WA AC AC

只有 40pts。

然后感觉从下表面入洞时要回溯，所以……

WA WA WA AC AC AC TLE TLE TLE TLE （30pts）

只能说分还少了。

然后想到一种很妙的方法可以避免 double 距离的精度丢失，就是把距离开平方，不过还是 30pts
问了老师，发现有这么几个问题：
1. 不开 long long 见祖宗
2. 事实上全部都不用回溯，因为一个洞搜完后如果再搜就还是一个结果
3. 在判断从下表面能不能入洞时，pw(a[i].z) <= pw(r * 2) 中的 pw(r * 2) 要改成 pw(r)（毕竟是一个面和一个球，不是两个球）

WA WA WA AC AC WA WA WA AC AC
又得到了 40pts 的好成绩。不过没有超时了。

改完后又有几个问题：
1. r 也要开 long long
2. 每组数据询问时要清空 vis 数组（毕竟数据和数据不能隔空影响……）

然后就 AC 了。

---

2024/10/14
P7775 [COCI2009-2010#2] VUK

哇塞，第一次见这种算法大礼包
解题思路：二分+DFS+BFS
思路比较复杂：
1. 先跑一个多源 BFS，执行前把每棵树的位置 Pos(x, y) push 进队列，然后 BFS，把每个点里树的最短曼哈顿距离存到 dis 里

2. 题目中有关键词“最小值最大”，易知使用二分的算法，二分最后的答案（也就是离树的最小值），如果合法的话向后找，否则向前

3. 二分的 check 函数是个难点，在 check 中调用 dfs 函数来搜索存不存在一条最短距离为 s 的路径（也就是任意时刻 dis[x][y] 值 >= s），并引用 flag 作为有没有找到这样的一条路劲，最后 check 只需要返回被 dfs 函数修改的 flag 就行了。

最开始二分 l 和 r 写反了，样例没过。经过老师的提醒后，改回来了。但是 50pts，测试数据：

AC AC MLE MLE MLE MLE AC MLE AC

经过老师研究发现，这个题内存限制 32.00MB（十分毒瘤），当时的入队重复判断是 "if (dis[xx][yy] < d + 1) continue;"，然后发现没有判断自己，重复入队导致队列爆掉了。把 < 改成 <= 就行了。

---


2024/10/4
模拟比赛 #5 T1 代码（cppcode）
这道题很简单，就是题目有一个没有表述清楚的点：空格算是不可见字符，还有输入 n 后有换行会被后面的 getline 读到，要 getchar 一下，把换行吸收掉。除此之外就没别的了（哇塞我竟然在题面有误的情况下 AC 了！好多人都因为这个于是代码死了……）

---

2024/10/4
模拟比赛 #5 T2 三七（one）

历史最强诈骗题。
首先，一定要看透题面以及数据的伪装，不少人看到题目就没有思路，写了个 dfs 结果爆 0。（幸好我没有中招）

题面的意思是将任意两个数模一下，这里可以贪个大心：将小的数模大的数。这样子总和不变！！！
并且题目也没说不行（也就是说部分分 2 说 10% 的数据保证最优异或三次，其实理论上只要复杂度优，dfs 这样做可以得满分）
于是最后的答案就是 a 数组里所有数的和

但是

注意到题目并没有保证所有数并不相同，也就是存在所有数相同的情况！（幸好我考虑了）
这是就拿 a[1] 一直模 a[2]，答案就是和 - a[1]（第一次 a[1] 变成 0，第二次还是 0，第三次还是 0），特判即可
多测不清空，家人两行泪。注意加和的 ans 每组数据要清空！！！

---

2024/10/5
P2010 [NOIP2016 普及组] 回文日期

调了半天暴力程序，结果超时，问了老师才知道只用枚举月与日，年反推就行，最后一定回文，判断一下越界了没有即可。注意由于闰年的 0229 反过来是 9220，能被四整除，是闰年，所以不用判断闰年，直接将月份数组的 2 月天数拉到 29 即可。

---

2024/10/5
T517629 【第十一周】黑不拉几矩阵

这道题好难，要打表找规律，是个数学题（n <= 1e9），连线性复杂度都不行，更别想按照题目的套路 n2 枚举了），打表程序还有问题，问了老师才解决，正确的打表了发现打出来的像一个一个 ‘井’ 字，然后推柿子：三行是 n / 3 * 2 + n（前两行 n / 3，第三行全是一），然后 n 里面有 n / 3 个这样的三行，接着处理余数，由于 n % 3 <= 2，所以跟全一的第三行无关，直接 + (n % 3) * (n / 3)，再就是去掉对角线，研究发现对角线也是 n / 3，最后 / 2 把左下角剪掉（因为没了对角线后左下和右上相同），最后就是：

n / 3 * 2 + n + (n % 3) * (n / 3) - n / 3 / 2

然后就得答案了。（不开 long long 见祖宗）

---

2024/10/7
模拟比赛 #6 帕鲁王国（king）

说实话这题还蛮有意思的，就是让你模拟一个计算机电路。看一下数据范围：n = 1e5。果然 T3。

直接爆搜每个输入引脚（Input）的状态，可以过第一个部分分（n <= 12），因为这样至多有 11 个输入引脚，所以只需要计算 pow(2,11)=2048 次，对于计算机来说简直小菜一碟。但是后面就很难办了。

注意到题目给出的一个性质，每一个逻辑门只能输出到编号大于他的逻辑门中，也就是有向（不可能 A < B, B < A）。而这也意味着不会存在 A -> B -> C -> A 的结构，因为不可能有三者的编号 A < B < C < A，所以无环。综上所述，有向无环 DAG，那不就是树吗？

不过先别急，如果这样做还是会超时，考虑将递归转成递推，利用 DAG 有向无环的特性，从第 n 个帕鲁枚举到第 1 个帕鲁，用 TRUE[i] 表示第 i 个帕鲁状态为 1 的方案数，状态为 0 的存 FALSE[i] 里面，则：（总方案数指的是所有状态，也就是对所有帕鲁 TRUE + FALSE 的值求积）

- 输入引脚：TRUE[i] = 1，FALSE[i] = 1
- 与门：       TRUE[i] = 所有接入帕鲁编号对应 TRUE 相乘，FALSE[i] = 总方案数 - TRUE[i] （与门 1 必须接入全部为 1）
- 或门：       TRUE[i] = 总方案数 - FALSE[i]，FALSE[i] = 所有接入帕鲁编号对应 FALSE 相乘 （或门 0 必须接入全部为 0）
- 非门：       TRUE[i] = FALSE[接入编号]，FALSE[i] = TRUE[接入编号]

最后输出对应的 TRUE[1] 或者 FALSE[1] 即可。

---

2024/10/8
P8814 [CSP-J 2022] 解密 （CSP-J T2 decode）

第一次遇见这么难的 T2。

简化一下题目中的式子，就是求正整数 p、q 满足 pq = n（设为 1 式），ed = (p - 1)(q - 1) + 1。先简化第二个式子：

ed = (p - 1)(q - 1) + 1
ed = pq - p - q + 1 + 1
ed = pq - p - q + 2
ed + p + q = pq + 2
p + q = pq - ed + 2
p + q = n - ed + 2 //结合 1 式

接下来把化简的这个式子与 1 式放在一起：
pq = n
p + q = n - ed + 2（设为 2 式）
好像不能继续求解了，这时我们来看看 p - q 与 p + q（以下 a ^ 2 代表 a 的平方、sqrt(a) 代表根号 a）

(p + q) ^ 2 = p + 2pq + q
(p - q) ^ 2 = p - 2pq + q
(p + q) ^ 2 - (p - q) ^ 2 = 4pq
(p - q) ^ 2 = (p + q) ^ 2 - 4pq
p - q = sqrt((p + q) ^ 2 - 4pq) //设为 3 式

通过 p - q 与 p + q 可以求出 p（p >= q）

p = 2p / 2
  = ((p - q) + (p + q)) / 2
  = (sqrt((p + q) ^ 2 - 4pq) + (p + q)) / 2 // 结合 3 式
  = (sqrt((n - ed + 2) ^ 2 - 4n) + (n - ed + 2)) / 2 //结合 2 式

求出 p 后 q 就等于 n - ed + 2 - p 啦！

这样的解法时间复杂度 O(1)，稳过 k <= 1e5 的数据数量，拿下满分。最后实现是要考虑细节的处理：

1. 不开 long long 见祖宗
2. p = 0（判断即可）
3. p = 无理数 （带入式子验证一遍即可）
4. p = 虚数 （sqrt 虚数会返回 NaN，强制转 long long 值为 0，被处理过了，所以不用考虑）

---

2024/10/15
P11186 三目运算

这道题首先不难发现问号表达式是一个递归式，可以写成这种形式：x[op]q?n1:n2。其中 op 为 '>' 或 '<'。
递归处理问号表达式，从字符串中读入 op, q, n1, n2，注意不能定义新字符串，也就是传参与保存子串时只能定义 l, r，不能 substr。于是就可以写出一个代码，我的代码通过改变 i 的值，层层递进，实现下标移动的效果，至于 ++i，是在进行 i = i + 1 操作后返回 i 的引用，所以可以作为左值。

但是如果这么写，只能拿到 64 分（q <= 10），剩下的测试点会超时，优化手段就是在代码开头预处理所有 x 的答案，定义 ans[i] 为 x 为 i 的答案，则可以对 l 到 r 之间的答案进行赋值，如果是数字，直接赋值，否则考虑 q 合法与不合法两种情况，进行递归。由于 m <= 1e5，又因为所有的 x > m 的答案都与 x = m + 1 的答案相等（一直大于），所以 ans 数组只用开到 1e5 即可，足够应付所有 x <= 1e9 的情况。这样就 AC 了。

---

2024/10/19
B4049 [语言月赛 202410] 平均分计算

这道题是一个恶心的大模拟，恶心到赛时竟然没调出来。（虽然后面还是不看题解过了）

思路很简单，首先封装一个学生结构体 Stu，用来存学生的学号（id），课堂码（uid），参与度（sc）以及排名（pm）。
- 学号 id（数据给出）：指的是在学校里学生的编号。

- 课堂码 uid（程序给出）：由于学号范围太大（1 ~ 1e9），不方便处理，所以在课堂里给学生一个专有的编号，称为 "课堂码"。课堂码是紧挨着排列的，不像学号那样分散，所以占用范围小一些（由于一个课只有 100 个名额，所以课堂码的范围是 0 ~ 100，比学号小了很多），在查询同学时很方便。

- 参与度 sc（数据给出）：指的是在课堂中学生的得分。

- 排名 pm（程序给出）：通过排序与并列处理，得到在课堂内学生的排名（依据参与度）。

然后封装一个课堂结构体 Lesson，用来存学生（Stu 类型的数组 s）与排名分值 w。
注意 Stu 类型还要定义两个比较方法（cmpid, cmpsc），顾名思义就是按学号排序与按参与度排序。
最后定义一个被除数 x 与除数 y，表示得分总和与课堂数量

一些基本的封装工作完成，接下来就是处理分数部分。
首先是输入，没什么好说的，唯一要注意的就是这个输入完学号后要立即排序学号，因为题目中的参与度是与从小到大的学号匹配的。
然后是处理，先判断一下报名的人中有没有小 Y，如果没有就跟结果没有关系了，跳过。然后将 y++（一门新的课），接着按参与度排序，排完序后统计排名，并列的处理方法就是判断参与度是否与上一个同学的一致，如果是的就将排名更新为上一个同学的排名。接着通过每个同学的 id 找到小 Y 在学生数组 s 中的下标。最后将得分（获取小 Y 的排名，然后通过排名分值 w 得到分数）累加进 x 里。

最后处理完分数后就是化简工作，先将除法（更确切的说，是分数）转化为带分数，然后求带分数中真分数分母与分子（x 与 y）的 gcd，令 x 与 y 同时除以 gcd 即可。

---

2024/10/20
（衔接模拟比赛 #8）T3 最小最大子段和（minmax）

赛时没有拿分，题目一堆形式化表达，又有点绕，没有看懂。赛后重新想了想，这道题是求一个序列被分为 k 段后的最大子段和的最大值最小是多少。（有点绕？"一个序列 · [[被分为 k 段后的 · 最大子段和] · 的最大值] · 最小是多少"）。求最大值最小，考虑二分答案。二分最小的最大值，于是答案就有了两种情况。

1. 有一种分割方法可以将序列分为 k 个部分并使所有分出来的部分的最大子段和不超过这个最大值（有点绕？"可以 · [[将序列分为 k 个部分] + [[并使所有分出来的部分 · 的最大子段和] · 不超过这个最大值]]"）。此时这个答案合法，保存答案并向下寻找。

2. 这个答案不满足 1 的条件，不合法，向上寻找合法的答案。

写完了二分后，就要看看判断答案是否合法的 check 函数应该怎么写。

考虑贪心，用 s 表示直到目前的最大子段和，t 表示分出来的段数，如果 s 超过了答案（设为 x），则将最大子段和保存为 a[i]（也就是将前面的归为一段，现在开始新的一段），最后判断 t 是否 < k，如果是的，就返回 1，否则返回 0。（注意题目要求分为 k 段就是分 k - 1 次，所以 t < k 是 < 而不是 <=）还有要注意，如果 a[i] >= x，就必须返回 0，因为这个数分到哪一段，哪一段的最大字段和就会超过 x。

最后就 AC 了（注意不开 long long 见祖宗）

---

2024/10/26

2024 CSP-J 复盘


T1 扑克牌（poker）

这道题其实挺简单的，题意就是你有 n 张牌，求还需要多少张牌才能凑成一副完整的牌。
考虑使用桶维护输入信息，将个每一行输入拆成两个字符，第一个花色处理成 1 ~ 4 的数字即可，第二个点数处理成 1 ~ 13 的数字就可以，开一个桶，每输入一个花色与点数就在对应位置标记一下，最后没被标记的有多少个就是答案。


T2 地图探险（explore）

题意就是你在迷宫中的点 (x, y)，每一个点有可能是墙（'x'）或是空地（'.'），你需要遵循右手法则移动（右手法则就是走得通就走，走不通就右转），问在 r 次移动后你会路过多少个不同的点。
这道题也蛮简单，只需要模拟即可。先在 vis 数组中标记点 (x, y)，然后循环 r 次，每次算出下一步的坐标 (xx, yy)，看是否不合法（走出地图或走到墙里），如果不合法，向右转（d = (d + 1) % 4），否则移动到那个点去（x = xx, y = yy）。

小技巧：可以先把每个方向的偏移量算出来存到常量数组里像这样：dx[4] = {0, 1, 0, -1}; dy[4] = {1, 0, -1, 0}; 然后再处理下一步的格子时就可以直接 xx = x + dx[i], yy = y + dy[i] 啦！


T3 小木棍（sticks）

这道题……
赛时没想出正解，直接想部分分，结果 70 分，该拿的都拿到了，结果当天洗澡时想出正解，第二天即在洛谷上 AC……
这道题是一个分情况讨论，我们要讨论每一种情况。
首先看到 '8' 这个数字，明显这个数字用的火柴棒最多。所以如果每一位都用这个数字，火柴棒消耗的会非常快，最终数字的位数自然就少（决定数字的大小时第一个要看位数）。所以考虑多用 '8'。
但这样可能会造成不能正好摆完的情况，所以我们分情况讨论火柴棒数量 mod 7 每种不同的余数所对应的情况（至于为什么要 mod 7，你后面会知道的）

-  n mod 7 == 0
这时我们发现最耗火柴的数字 '8' 正好就是 7 根火柴棒才能摆成。所以全部摆 '8' 即可。由于这个输出方法要用到很多次，所以把它封装为一个叫 "put8()" 的函数。

-  n mod 7 == 1
由于 '8' 要多用，所以我们要想办法将 n 变为能整除 7 的数。最好的办法就是先输出 "10"，这样 n 就耗去了 2 + 6 = 8 根火柴棒。稍微推导一下就发现此时 n 就是 7 的倍数了，于是直接 put8()。

-  n mod 7 == 2
先输出 '1'，再 put8()。

后面的以此类推。要注意的时 n % 7 == 3 时如果 n 是 10，就要输出 "22"，其他情况是先输出 "200"，再 put8()。这个特判建议放在最开头 while 的后面。还有一点要注意，n = [1, 7] 的情况要开个数组离线存答案：{0, -1, 1, 7, 4, 2, 6, 8}。（数组最开头的 0 是用来占位的）

然后就 AC 了。

---

2024/11/5
P1057 [NOIP2008 普及组] 传球游戏

其实是一道简单的 dp，不过自己竟然没自己想出来，实在可惜。
首先分析样例：
我们将三个同学编个号，再做成一个环。我们用数字代表每个同学得球的可能性。（设小蛮为 1 号）

编号:   1 2 3 1 2 3
        -----------
第一轮: 0 0 0 1 0 0
第二轮: 0 1 1 0 1 1
第三轮: 2 1 1 2 1 1

很明显，小蛮有 2 种情况得球（1 -> 2 -> 1，1 -> 3 -> 1）。而二号有一种（1 -> 3 -> 2），三号也有一种（1 -> 2 -> 3）。
容易发现我们在推导时每一种同学在这一轮中的可能性 = 左边同学在上一轮中的可能性 + 右边同学在上一轮中的可能性。
也很好证明，毕竟只有他左边和右边能传到他，所以要么从左边传，遵循左边的可能性，要么从右边传，可能性与右边一样，左边或右边，用加法原理，就是左边可能性 + 右边可能性。注意一定是上一轮的左右两边，否则可能会导致一轮连续传多次的情况。（这里不能压成 1 维，因为 1 维有后效性），最终的转移方程为：f[i][j] = f[i - 1][j - 1] + f[i - 1][j + 1]。其中 f[i][j] 表示在第 i 轮中第 j 个人的得球概率。在实际处理时要特殊判断 1 号与最后一号的情况，因为是一个环，1 号的左边是 n 号，n 号的右边是 1 号。
然后就 A 了这道 dp 题。

---

2024/11/6
P2758 编辑距离

考虑使用 dp 来解决。
设 dp[i][j] 为字符串 A 的前 i 个转化为字符串 B 前 j 个的最小次数。则有两种情况：
- a[i] == b[j]，此时不需要进行任何操作，dp[i][j] = dp[i - 1][j - 1]
- a[i] != b[j]，dp[i][j] = min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1])。相当于 min(删除，添加，替换)。
然后就 A 了。

---

2024/11/26
csp-j.com P683 饮水工程

题意简述：已知有 n 个区域，每个区域可以从另一个区域引来水，也可以自建水库，当然，每种操作都有本身所需的费用，求给所有 n 个区域供水的最小花费。

考虑自建水库费用一定普遍高于引水费用（直觉），所以先对原图跑一个 MST，然后只需要其中一个节点自建水库就可以做到全部供水。
当然我还是太天真了。
由于我的直觉是错的（考试千万别相信直觉），所以仅有 9pts。后来发现可以将“自建水库”视为一个特殊节点，编号 n + 1，如果哪个节点要自建水库，就从那个特殊节点引水过来，可以证明这是对的，MST 最后会将所有节点连在一起，巧合的是就算是 n 个节点引来引去，也需要一个节点自建水库，这就非常符合逻辑了。
当然我依旧太天真了。
这道题有一个非常离谱的坑点：p 是有向图。也就是说，从 i 引到 j 和从 j 引到 i 效果一样花费不同（非常反直觉对吧，考试千万别相信直觉）。不过解决方法很简单，只需要对两种情况取个 min 就行。
然后就 AC 了。
第一次提交，MLE，仅有 9pts，发现数组开小了，又交一遍，WA，9pts，然后多测不清空家人两行泪，直接 RE（原因是没有正确使用指针和 memset），改完后还是 9pts，最后才发现思路不行。后来想出特殊节点的思路，大改一通，还是 9pts，又检查了其他的，包括也问了 AI，都没检查出来，AI 也说是正确的，结果提交又是 9pts。我都给整懵了。最后问老师才发现是有向图，然后终于 A 了。

---

2024/12/8
P11247 [GESP202409 六级] 算法学习

考虑贪心（没想到是贪心 QWQ）
同一个知识点的题我们优先做含金量高的题，也就是掌握程度从大到小排序。在此之前，为了方便管理，我们将 b[i] 存在一个 vector 中，像标为他对应的知识点编号，找到下标对应的 vector 后直接塞就行。
对于每个知识点，先大到小排序，排完后计算 tmp（总共的掌握程度）和 cnt[i]（做的题目数量）。For 循环遍历每道题，先在循环里计算两个数，然后再在循环里判断达到掌握程度达到 k 了没有，达到了就 break，没达到继续。最后如果题目遍历完了都没达到 k，那就说明整个题库都不够他做，直接 -1，return 0。没有就记录答案，先记录需要做的题目总数 ans（ans += cnt[i] 即可），然后更新 maxn（最大题目数量）和 maxidx（maxn 对应的下标）。
遍历完知识点后，先看 maxn 能不能与剩下的题交替做避免重复（ans - maxn [其他的题] >= maxn - 1 [maxn，要 -1 是因为交替可以少一个]），如果能就输出 ans（做题总数）。如果不能与剩下的题交替做避免重复的话，就多做点题挽救一下。定义 sum 表示总题数，凡是不是 maxn 的题都做（做过的题不要做，实现方法：剩下的题 = 总数 - 做过的题，sum  += q[i].size() - cnt[i]）。最后判断加上这些够不够，够输出 maxn + (maxn - 1)（交替最小数目），不够就无可救药了，输出 -1 即可。

---

2024/12/12
csp-j.com P249 最短路上的统计

拿到题目先别慌，仔细看一看。数据范围 n <= 100，最短路算法随便用。考虑题目要求所有最短路径上的点总数，不难想到先求出最短路（floyd），再一一枚举 k, i, j。看 i 到 j 的最短路是否经过 k（dis[i][j] == dis[i][k] + dis[k][j]）。如果是就 ans[i][j]++（i 到 j 最短路节点总数 + 1）。最后对于 q 个 u 和 v，直接输出 ans[u][v] 即可。
最开始傻到极致的我尝试用 dijkstra，入队时就 + 1，仔细想想就知道无法判重，如 1 - 2 - 3 - 4 和 1 - 2 - 5 - 4 同为两条最短路，1, 2, 4 却被重复计算，导致了答案数量过多。问了老师，在老师的开导下，想到 floyd 的方法，但是我最开始是边求最短路边计算答案，所以不能保证节点在最短路上，后来又被开导了一次，才想到这种方法，以后准备复刷这道题，因为说实话我实在不服气。

---

2024/12/15
csp-j.com 25 提高预科班模拟赛（1）
- A 徐老师的送分题
这道题没啥好说的，A + B 等于 1 时输出 2，否则输出 1 即可 AC。

- B 徐老师的玩具计划
设 dp[i][j(0/1)][k(0/1)] 为当前第 i 个选择状态为 j，第 i - 1 个选择状态为 k 时的答案，易得：
dp[i][0][0] = max(dp[i - 1][0][0], dp[i - 1][0][1]);   //i - 1 不选时的情况 dp[i - 1][0][0/1] 取最大值，i 不选所以不加 a[i]
dp[i][0][1] = max(dp[i - 1][1][0], dp[i - 1][1][1]);   //同上，只不过要选 i - 1 所以 dp[i - 1] 部分略有不同，可以自行理解
dp[i][1][0] = max(dp[i - 1][0][0], dp[i - 1][0][1]) + a[i]; //同上上，要加 a[i] 因为选了 i
dp[i][1][1] = dp[i - 1][1][0] + a[i]; //注意不能选三个连续的物品
初始化：
dp[1][0][0] = 0; //啥也不选只能 0
dp[1][0][1] = 0; //i - 1 没有意义，所以还是 0
dp[1][1][0] = a[1]; //选了就是 a[1]
dp[1][1][1] = a[1]; //同上
然后就 AC 了。
赛时调了 dp 样例错误，比赛即将结束，只好写了个 DFS。

- C 徐老师的爬塔游戏
floyd。
存边就是一大堆数学公式往里一套，加上一些判断和 fabs（赛事因为这玩意调了好久）即可完成存边，存边是最难的。然后就是最短路 floyd 轻松搞定，代码好写，AC~
赛事样例 AC，自信满满，谁知错误不断，只有 20pts，寻找老师，发现忘记除以 v 在末尾（😭），除了 v 又发现自由落体不是距离是时间，又给自由落体乘了 v（😭），反正就是考试粗心，样例太水 v = 1，😭😭😭😭😭（差点就有红包了）。

---

csp-j.com P452 圣诞岛的旅行
无奈做出来时比赛提交入口已经关闭了 QAQ
n = 100 容易想到 floyd，谁知由于 floyd 的神秘特性导致无法保证每个点只去一次（什么不明白？自己举反例去吧），调了半天的 floyd，最后才发现是老派最短路 dijkstra。
邻接表，有向图，队列，步步轻松。
最短路，最长路，重载，步步 easy。
就是注意各种不要搞混，如 diss disl,viss visl,Nodes Nodel, qs ql（就因为这调了 nan 小时）。
然后 TLE，开个 vis 即 AC。

---

csp-j.com P262 挤牛奶
dp 题。

dp 四要素：
状态：dp[i][j][k] 在选到第 i 个奶牛，累计选了 j 个奶牛，差值为 k 的情况能(1)否(0)实现。
转移：dp[i][j][k] = dp[i - 1][j - 1][k - w[i]]/*选第 i 头*/ || dp[i - 1][j][k] /*不选第 i 头*/
初始化：dp[i][0][0] = 1
答案：min(abs(i - (sum - i)) （需要 dp[n][n / 2][i] 为 1）)

AC。
最开始状态搞错，20pts，然后改了状态，改了负数下标、初始化以及答案的问题，就 AC 了。

---

ABC389 Squares in Circle
这道题……
考场上没调出来，难得要命，是个数学题。
考虑双指针（啊
把一个圆分为横竖两条坐标轴和其他四块。横竖两条坐标轴的中点有 1 个，一条方向上的除了中点的点的数量，还有 R - 1 个点。总共就有 4(R - 1) + 1 个点。
其他四块我们只考虑一块（右上角），设 j 为这一块第 i 行（从下往上）的正方形数量，则 (i + 0.5)^2 + (j + 0.5)^2 = R^2。（代码里扩了倍是为了兼容 long long 类型）我们发现，i 越大，j 越小，不会出现某一行使得这一行的 j 比上一行大（你们见过那样的圆形吗）。所以双指针 j 即可，时间复杂度 O(2n)。

---

P4653 [CEOI 2017] Sure Bet
一道看起来绿实则黄的题
这道题的题目大意是给你两个长度为 n 的序列，你可以选任意个数字（设为 k 个），答案为你选的两组数字每组数字的和的最小值减去 k，求最大答案。
我们的目标是最大化最小值（是不是很眼熟？可惜这道题正解不是二分是三分），所以我使用的不是正解，而是贪心（
性质一：考虑到对于每个物品，我们肯定选最大的，因为这样子不会增加代价，而且可以使收益上升。所以先将 a,b 排序再说
性质二：设 sumA/sumB[a] 为在 A/B 组里选择 a 个物品的收益（不计算代价），则 sumA/B[i] = sumA/B[i - 1] + a/b[i]，前缀和！
性质三：当在原基础上在 A 组选第 i 个物品时，收益为原来的收益加上 a[i] 的收益减去 i，而对于 B 来说不会有 a[i] 带来的收益。
性质四：由性质三可得，为了使最小值最大，我们要在目前收益少的那组选东西。故考虑双指针。（说好的贪心
*注：在某种程度上，这道题的解法本质是贪心，但是用了双指针的优化方法而已。
然后悟到了贪心双指针，结合性质这题就 AC 了。
最开始写一份代码，输出 0，调试一看收益全是负的（？），然后一看代码没排序，加了个排序，还是不行，上下看左右看死看硬看，最后发现 从大到小被我弄成了从小到大（cmp 里写的 b > a），然后就 A 了。

---

ABC391 D - Gravity
俄罗斯方块，考场上有思路，因为复杂度问题而没有敲代码，结果赛后才知道原来自己复杂度算错了😂
封装一个 Block 结构体，将第 i 个方块保存到 a 数组里，将 a 数组按照高度从低到高排序，将每列的方块数量取最小值得到要消除多少行。对于每一行，通过高度取 max 值得到消除时间，然后枚举这一行的每个方块将答案保存进编号对应的 dis 数组里，最后直接通过 dis 回答问题即可。

---

P1578 奶牛浴场
我谢，能刷到这题也真是幸运。说好的一天 3 Questions 结果看到一道诡异的蓝以及恐怖的“奶牛”字眼（要知道 USACO 出的全是烧脑题），当场表示做完这道我不做了。不过靠着我强大的****，还是给他硬啃下来了。

首先，要做这题先得做玉蟾宫那题，你要有一个悬线法的基础，然后我们横竖扫一遍（Sort First!!!）（我们在此只说横的不说竖的），枚举任意两个产奶点，然后计算以这两个为左右边界（上下边界实时变更）的答案。最后更新一下上下边界，因为我们刚才的产奶点要进入矩形了。上下同理（Sort First!!!）。
本次第一次提交：MLE 3pts，原因：想使用玉蟾宫的悬线法，但是 3e4 的边长以及 12500MB 的内存限制实在是有点打脸。当场崩溃，我勒个二维浴场数组都开不了，别说计算答案了，连产奶点都识别不了，更不能悬线！！！！！

本次第二次提交： TLE 0pts，原因：尝试暴力被打脸
本次第三次提交：TLE 1pts，原因：书上说过双指针，被打脸
本次第四次提交：WA 7pts，原因：看完题解觉得横着扫一遍就行了，结果被打脸。这个是样例太水了，浴场和围栏都是正方形，QAQ
本次第五次提交：WA 7pts，原因：变量名写错大于小于写反循环顺序写反多余特判横竖xy搞反上下updown搞反出现负数没特判正方形没特判
本次第六次提交：AC 100pts，原因：从头到尾跟博主对了一遍。

---

2025/2/7
P3467 [POI 2008] PLA-Postering
大意：有 n 个长 d[i] 宽 w[i] 的矩形在地上放在一起，求至少使用多少个矩形能将他们正好覆盖。
首先 ans 最大就是 n，因为完全可以每个矩形单独一个矩形覆盖。然后如果不考虑宽度（事实上宽度与答案半个估值的关系都没有）考虑 i 和 j 什么情况下可以被相同的一个矩形覆盖住。
1. i ~ j 范围内所有矩形高度相等。

e.g.
+--------------+ ## #
|##############| ####
|##############| ######
|##############| #######
+--------------+
  ^                      ^
  i                      j

2.i + 1 ~ j - 1 范围内的所有矩形都不低于 i 和 j 且 i、j 高度相等。

e.g.
  #    #    #    #     #
  ##  ##  #        ##         #   #
  ####### ##  #      #   # # #   #
+---------------+ # ## ## # #  ##
|###############| ######### #####
|###############| ###############
+---------------+
 ^                        ^
 i             j


如果说恰恰相反：

         #
       # #          #
 #   ##  ##  #     ##
 #   ######### ##
+-------------+
|#############|
+-------------+
 ^           ^
 i           j

可以看到虽然 i 和 j 高度相等（i = j = 3），但是因为 i 和 j 之间有比 i 和 j 小的，所以 i 和 j 不能被同一个矩形覆盖。
换句话说，在能否一起被覆盖以减少矩形数量这件事上，高度矮的最有决定权。
所以可以看到，每当我们枚举到一个矩形，那么他前面比他高的就一点作用都没有了，直接扔到垃圾桶。那么这样子很显然我们最后的序列一定是从矮到高单调的。既然每次都是对序列尾部操作，序列里越靠前越宁静，所以很明显单调栈即可。每次单调栈，枚举到新元素，栈顶比他高二话不说栈顶直接扔垃圾桶（stack.pop()），最后压进栈顶，如果这个新元素与栈顶的高度一毛一样，ans--（可以一起被覆盖以减少矩形数量），然后继续 pop，直接 AC。

---

2025/2/8
P1725 琪露诺

DP × 单调队列
废话不多说，维护一个以 DP 数组为依据的单调队列（dp[i] 表示跳到 i 的最大值，转移方程贼简单），单调的时候看能不能跳到对岸，能就更新答案，AC。虽然是绿但只是实现比较复杂，思考没啥含金量。

---

2025/2/8
P1719 最大加权矩形

二维前缀和模板（我不讲了，想了解自行小鲸鱼），由于常数小，可以勉强卡过 n = 120 的数据，获得 100 的好成绩（疑似正解？）。

---

2025/2/8
P1314 聪明的质检员

作为一道 NOI 的题，数学符号较多。简单解读一下题目里出现的数学公式：我们令重量大于 W 的为“合格品”，那么第一个两个 Σ 相乘的那个是合格品的数量乘以合格品的总价值（下文称为总和），后面那个有 l 有 r 的是 | 每个区间里的总和 | 的和。理解之后发现首先 y <= s（不然你费那么多力气干什么），然后发现有单调性，所以二分 W 的值，每次计算总和的时候提前前缀和，最后输出答案即可 AC 本题。

---

2025/2/11
ABC392 D - Doubles

首先，如果要做这道题，要对“概率”有一定认识。然后，输入的时候顺便排序 j 上的元素在数组 a[i] 里。接下来直接统计每个数字出现的个数，将数字与个数打包成一个 Node 结构体然后存 t (TYPE: vector <ull> []) 数组里。最后计算答案，枚举任意两个骰子，概率分母是两个骰子的数字数量乘积，分子双指针（因为排序过了），每个相同颜色的贡献为数量乘积，最后一算，Ended!!!
这道题唯一非常坑爹的地方就在于所有整数都得 unsigned long long（0 ~ 2^32-1），不然你就会有一个诡异的测试数据 WA 掉。

---

2025/2/16
P1884 [USACO12FEB] Overplanting S

题目大意：有 n 个矩形，给出每个矩形左上，右上，左下，右下的点的坐标，求总覆盖面积（重复覆盖不算）。
思路：离散化 + 二分
首先，这道题的思路与一维的区间求和很相似，将读入进来的点离散化（排序，去重，编号），然后就是计算总覆盖面积（这道题的数据范围不是很友好，需要差分，方法就是枚举长方形的上边界到下边界，然后将每一行中属于长方形的一排差分，也就是左端点--，右端点++，时间复杂度 O(n^2) => O(n)，最后的时候每排前缀和一下即可），最后枚举每个位置，如果不是 0（题目中没有要求计算重复），就把面积（(c[i + 1] - c[i]) * (c[j + 1] - c[j])， 也就是用下一个格子的坐标减去这个格子的得到长度）加到答案里，最后输出答案即可。

---

2025/2/23
ABC393 D - Swap to Gather

Math.
首先我们可以将每个一的位置存下来，a[0] = 第一个 1 出现的位置，a[1] = 第二个 1 出现的位置...a[i] = 第 i + 1 个 1 出现的位置。然后发现移动的时候相对位置不变，否则必然不是最优解，比如移动后原来第 2 个 1 的新位置后不会跑到第 1 个 1 的前面。设 x 为移动后连续 1 的开头位置，则第 1 个 1 要移动到 x，第 2 个 1 要移动到 x + 1，则总距离为 |a[0] - x| + |a[1] - x - 1| + ... + |a[i] - x - i| + ... + |a[last] - x - last|。将每一项的 a[i] 与后面的 - i 合并使 a[i] -= i（可以理解为 b[i] = a[i] - i; a[i] = b[i]），然后原柿变为 |a[0] - x| + |a[1] - x| + ... + |a[i] - x| + ... + |a[last] - x|。简单思考过后发现 x 取 a[i] 的中位数可以使结果最小，故排序 a[i]，然后取 a[len / 2] 作为 x，计算答案并输出即可。需要开 long long。

---

2025/2/23
YACS 2025-2 月赛 T3 区间求和

背景说看起来是前缀和，做完题后发现其实真的是前缀和。（夹了一点差分，不过不影响口感）
利用差分求出每个位置会被加多少遍，然后排序，则越靠前加的次数越多，将数组排序然后一对乘起来一加即可，这道题的做法不是很喜欢 memset，数据比较毒瘤，所以要绿必须将循环合并，边输入边处理边初始化边计算答案（就是卡的这里）。然后就 AC 了。

---

2025/2/25
P3017 蛋糕切片

二分前缀和，前缀和，二分答案，check 贪心，枚举右上角，不够往右，全部不够往下，前缀和统计答案，最后行数必须 >=题目给出的行数。即可 AC。

---

2025/2/25
P3406 海底高铁

我是五年级小学生，（What is IC Card? What is "扣" means? What is "博艾元"? What is "工本费"? What is "充值"? Why we have to "充值" before use IC card? 以上没有用翻译，自我欣赏英语水平）（龟速逃）这道题对刚学 OI 接触了 main() 函数的同学比如我不太友好。但理解题意以后简化一下就是每一段要么充值买 IC Card 然后一劳永逸每次经过都扣一下 IC Card。第二种就是直接经过时 A[i] 乖乖付出去。所以答案的计算考虑使用差分计算每个站他到了多少次（Uim: 这一站又是那么眼熟，我去过 1e5 次了好像）开个 ull（坑爹），AC!!!

---

2025/3/3
P2882 [USACO07MAR] Face The Right Way G

这道题正解是双指针×贪心，考虑到每次翻转都要从第一个 0 开始，所以枚举 0，然后双指针加桶开始枚举区间统计最小值 AC!!!。

---

2025/3/8
P4375 [USACO18OPEN] Out of Sorts G
世界最强结论题。

看看颜色，妈呀太 #00F 了。不过其实推完后，代码不是很长。
题目给出的是一种变异的冒泡排序。在每一轮冒泡中，他会先把大的冒泡，再冒泡小的。问题是排序会执行多少次。直接分析这种双向排序算法比较困难，不过可以自己手写几个样例看看发生了什么。
比如：
 2 5 4 3 1
首先进入循环时就会输出一个 moo（坑了不知多少人）
 moo x1
然后冒泡大的
 2 4 3 1 5
可以看见，5 跑到 3 的前面去了
接着冒泡小的
 1 2 4 3 5
可以看到，1 跑到 3 的后面去了
一轮结束，moo x2
我们发现，一轮中，冒泡大的会使一个大的元素跑到在他后面某一个比他小的元素的后面，而冒泡小的反之亦然，可以是一个小的跑到前面一个大的的前面。换句话说，会出现一个哨兵元素（在例子中是 3），使得他前面比他大的的元素跑到后面，后面比他小的跑到前面（有点快排的感觉了）这样算一轮的话，全部跑完要左右取 max 轮。于是，答案就成为了取一个使这个 max 最大的哨兵。（为什么是最大呢，因为排序完后你就不能找到一个哨兵了，而且可以证明，最终使用的哨兵只有一个，因为排完后每个把自己作为参照系的哨兵都会认为自己的位置没动。所以取最大的哨兵即可）AC!!!

---

2025/3/11
P1045 麦森数
温馨提示：不是正解，不是正解！！！（不过算了，时间复杂度与空间复杂度都正确，算是另一种解法）
题目选择的比较多的解法是：快速幂×压位高精乘。不过代码长，100 行，受不了，于是寻找其他办法。
首先解决第 1 问。2 的幂的特征是尾数是 2, 4, 8, 6，不可能是 0，所以题目求的 2 ^ n - 1 与 2 ^ n 的位数是相同的！推一推柿子，我们考虑到 10 ^ n 有 n + 1 位，而 n = log10(10 ^ n)，所以 10 ^ n 有 log10(10 ^ n) + 1 位。故 x 有 log10(x) + 1 位，那么 2 ^ n 也有 log10(2 ^ n) + 1 位，根据对数的基本性质，这个柿子可以转化为 2 ^ n 有 log10(2) * n + 1 位（向下取整）。（现实中请强制转换为 int，因为对数的结果是小数）
第 1 问解决，看第 2 问，直接 1 << n - 1 肯定不行，肯定会爆炸（连 n 都要开 unsigned long long，你还指望这个）所以我采用了与高精相似的数组。然后将第 1 位设为 1，不停乘 2，乘 n 次即可。不过这样会超时，如果每次都乘很大的数，数组就会爆炸，于是在我不懈的计算下，得到了一个既不会超时又不会爆炸的黄金值：2 ^ 60。算一算，n 有 3100000，3100000 / 60 = 51666，不可能超时。每一位最大是 9，9 * 2 ^ 60 只有 1.0376293541461623e+19，开 unsigned long long(1.84467440737096e+19) 正好装下，嘿嘿，果然数学是 YYDS（划掉）。每次每个数字的二进制都直接挪 60 位（就是乘 2 ^ 60），如果剩下 n 不够 60 就挪 n 位，然后处理进位，每轮循环 n - 60（因为每次都挪了 60 位）。最后减一（一定要减一！答案要求 2 ^ n - 1），然后按照格式输出就可以。

---

2025/3/11
P1228 地毯填补问题

分治：分而治之。
将方格分为 4 部分，其中一份有公主，用 L 形地毯在中间将另外三份覆盖，递归即可。错误原因：遍历顺序不符，看了看，我的递归顺序与坐标操作是反的，改了一下又有重复的，再改就过了。

---

2025/3/16
P3509 [POI 2010] ZAB-Frog

蓝题：你好
这道题是单调队列 + 倍增。

Part1
单调队列计算青蛙跳到哪里，考虑将青蛙的跳跃区间单调队列一下，给一个恒定的长度 k, 从 1 到 k + 1，距离最远也只能是两端，如果 r + 1 比 l 到 i 的距离都短，说明实在太靠前了，往后挪，否则更新答案即可。

Part2
倍增计算青蛙的跳跃情况，每次让青蛙跳 2 ^ i 下，直到 m 次末尾为奇数，需要减一下才能跳，就更新，时间复杂度巨少。最后输出答案即可。

---

2025/3/23
ABC397 D - Cubes
神级数学题，无题能敌。

题目就非常数学，给你一个 N（大的离谱，只有 long long 才能存），求满足 x ^ 3 - y ^ 3 = N 的任意一组 x 与 y，无解输出 -1。
推导过程如下，首先对 x ^ 3 - y ^ 3 一顿猛拆：
N = x ^ 3 - y ^ 3
  = (x - y)(x ^ 2 + xy + y ^ 2)

将 x - y 设为 a，将 x ^ 2 + xy + y ^ 2 设为 b：
   x ^ 2 + xy + y ^ 2 > x ^ 2 - 2xy + y ^ 2
=> x ^ 2 + xy + y ^ 2 > (x - y) ^ 2
=> b >= a ^ 2
=> N = ab, So N >= a ^ 3
=> a ^ 3 <= n

这样子就方便了，无论 N 有多大，哪怕 long long 才能存，在三次根前还是显得很弱小，所以只要能求出 x y 与 a 之间的关系，就可以通过直接枚举 a 来通过本题！
于是……

   N = ab
=> b = N / a
   a = x - y
=> x = a + y
   b = x ^ 2 + xy + y ^ 2
=> b = (a + y) ^ 2 + (a + y)y + y ^ 2 // 换元
=> b = (a ^ 2 + 2ay + y ^ 2) + (ay + y ^ 2) + y ^ 2
=> b = a ^ 2 + 3ay + 3y ^ 2
=> b = 3y ^ 2 + 3ay + a ^ 2
=> 3y ^ 2 + 3ay + a ^ 2 - b = 0

套一套求根公式……
[ax ^ 2 + bx + c = 0]
[3y ^ 2 + 3ay + a ^ 2 - b = 0]
a: 3
b: 3a
c: a ^ 2 - b

开求！
y = (-b +/- sqrt(Δ:(b ^ 2 - 4ac))) / 2a
x = a + y
将 a b c 的值带入后求值即可！！！AC！！！

---

2025/3/30
P1120 小木棍
蓝色的搜索还是第一次见。
这几天好久没写复盘都在研究这道题
Colour: Blue（提高 + / 省选 -）
Algorithm: Deep First Search, DFS（深度优先搜索）
我是认真的。

首先若使用裸体 DFS，你将得到 26pts 的好成绩。
如何剪枝：

1. 从大往小排序，因为小的木棍使用起来更灵活，大的使用起来更快，所以先用大的铺垫，再用小的填补
2. 同理，每次一个木棒整完了以后就从剩下的中最大的一个开始，原因一样，可以定义 first 为范围左边界方便处理。
3. 将 DFS 的枚举顺序设置为从小往大寻找解，找到解以后可以立刻输出并通过 exit(0) 正常结束。
4. 拼成的木棍长度必须不小于最大的木棍（不然最大的木棍就塞不进去）。
5. 拼成的木棍长度必须能整除所有木棍长度和（不然怎么都有剩的），可以枚举到长度和 / 2，每次判断，最后无解时就可以直接输出长度和。
6. 定义 nis 数组存储第 i 个位置后面第一个不等于 a[i] 的位置，每次小木棍塞不进去或者方案不可行就可以直接跳到 nis[i] 跳过所有与 i 相同的不合法的木棍
7. 如果木棍能正好把剩下的拼完，那么这个木棍的最优选择就是放这里，这里最优也是放这个木棍，后面却失败了，说明是这里这个木棍补完之前就有问题，直接 return。
8. OI 中比较常用的优化，为防止一个木棍选多次，使用 vis 数组记录用过的木棒，后面就不考虑这个木棒，防止重复，回溯时在还原即可。

8 个剪枝，每个剪枝贡献自己的一份力量，使程序快上几毫秒，最后确保时间可以直线起飞了 ●'◡'●
OI 的一大组成部分就是剪枝，多学一点剪枝手段，看似是卡时间才会用的歪门邪道，其实真正身处考场，一个剪枝，几十毫秒，说不定就 AC 了呢？

---
2025/3/30
P1763 埃及分数

紫色的搜索！！！！！！！！！！！！！！！！！！！！
Colour: Purple（省选 / NOI-）
Algorithm: Deep First Search, DFS（深度优先搜索）
没错我还是认真的

老规矩，做题先得裸搜来理解题意，但是我们裸搜时遇到了亿点点问题：埃及分数万万千，可以使用 10，100，1000，甚至无穷大个埃及分数凑。所以问题1：没有搜索范围。
那既然搜索范围可以无穷，那么一路走到底的深度优先搜索肯定没救了（因为根本撞不了墙，回溯不了！爆堆栈）
那就一层一层搜的广度优先搜索呗。可是，要知道，这棵搜索树不仅深度没限制，广度也可以无穷，因为你并不知道眼前的这些埃及分数哪些不应该选，而埃及分数又有无穷个，所以广度可以无穷，广度优先搜索也一败涂地（因为不知道何时弹出元素，只知道加入新元素！爆队列）
那咋办呀？？？

既然没有限制，我们就手动限制！
我们将限制深度一步步抬高，从 1 到 10，并且限制下一层的埃及分数必须比上一层小（分母比上一层大），那么这时，深度的限制设为 dep。
深度的限制很明显，那广度的限制呢？
由于最后要凑出 a / b，再加上我们越来越小的限制，可以看到，设当前埃及分数为 1 / i，所在层数为 h，那么到最后，埃及分数不会超过 1 / i * (dep - h + 1) = (dep - h + 1) / i（每一层不超过 1 / i，还剩 dep - h + 1 层）带上可行性剪枝的思想，我们的凑出分数为 a / b，而计算过后能凑出的分数为 (dep - h + 1) / i，那么 (dep - h + 1) / i 必须不小于 a / b。所以枚举的子节点必须小于 (dep - h + 1) * a / b，这就是我们广度上的限制。
所以，我们成功的把一个无限的问题变成有限的问题了，接下来只需要枚举深度的限制从 1 到 10，DFS 即可，细节我就不讲了。

这种将无限的搜索变成有限的搜索的算法被称为：
Iterative Deepening Seach，IDS  或者
Iterative Deepening Deep-first Seach，IDDFS
中文名：迭代加深搜索。
一种基于深度优先搜索的、新的搜索算法。

---

2025/4/5
P1379 八数码难题
终于来了点绿色，好清新啊
这次是 bfs，上次是双向 dfs，这次来双向 bfs
方法类似，只不过因为这道题的特质，所以更适合用 bfs 做
首先，上次我们是先搞前半部分的 dfs，把结果存下来，再搞后半部分
不过这一次，因为作为一个 3x3 的矩阵，要做到把答案存储下来实在比较困难
但是因为 bfs 那神秘的特质导致我们可以前后 bfs 同时进行！！！
原理很简单，我们将 3x3 的矩阵改成一个 9 位数（如果首位为 0，7 位数模 8 次模数最后为 0，不会出现异常情况！），然后我们把这个 int 与一个 bool 封在一起，bool 代表我们的方向是从初始状态往后推还是从目标状态往前推，这样就可以实现双向进行！最后如果两种状态相同且方向相反，直接将两方步数相加与答案取最小值，最后输出答案，AC！！！

---

2025/4/6
P5507 机关
又来到了诡异的蓝色
这道题是 Astar 算法，我们 G 的计算方法和 F* 的计算方法不多说了，H* 的话就随便取个乐观又接近实际的值，比方说与目标状态的差的一半（为啥要一半呢？是因为每次你一拧就可能驱动另一个按钮旋转），然后 Astar 即可。结构体里可以自动计算 F* 的值，注意要写比较函数因为需要堆。

---

2025/4/7
Q: 证明在 F* 比 F 严格乐观的情况下，A* 算法最先搜索到的解是最优解。
A: F* 比 F 乐观。使用优先队列，最先搜索到的解的 F* 比后搜索到的 F* 要更优。采用广度优先搜索的方法，先搜索到的一定比后搜索到的更优，除非在同一层。如果在同一层，则 F 相同，F* 更优，那么先搜到的即为最优解。

---

2025/4/7
Q：计算广度优先双向搜索的复杂度
A: 设 m 叉树有 n 层，双向搜索有 2 个 n / 2 层的 m 叉树，故复杂度为 O(sqrt(m ^ n) - 1)，是普通广度优先搜索的平方根级别。

---

2025/4/13
ABC401 D - Logical Filling
这道题主要是贪心。
首先拿到序列的第一件事是填一些很容易看出来的 ?。
比如以下这个：
o?.?o
两个 ? 都与 o 相邻，故不能填 'o'，只能填 '.'
如果这个时候 o 的数量已经达到 k 了，就说明填完了，把剩余的 ? 替换成 .，输出即可。这时就能结束程序了

否则的话……
首要的空填完后就是贪心了。
虽然题目在 o 的数量上有限制，但是现在我们假设可以填非常非常多个 o
那么，对于这种情况：
.????.
第一种方案是填 .o.o，这样子不会有 o 相邻且 o 数量最多
第二种方案是填 o.o.
很明显，一种说 .o.o 还有一种说 o.o.，所以这些问号填啥我们不确定，就不填了。
这是偶数个连续的 ? 的情况，我们得出的结论是，对于这种情况，我们无法确定，所以保留 ?

那有人说了，如果是这种情况：
.????o
那不就只能填 o.o. 了吗
其实这种情况已经被我们最开始的处理 pass 掉了
这种字符串给到我们的程序上时，第一步会把 o 旁边的 ? 变成 .
也就是说到这一步时，字符串已经变成了
.???.o
也就是说，我们预处理后，所有 ? 都被 . 包围了

那就引入了我们的第二个问题：? 连续数量为奇数，怎么办？
例如
?????
这时只有一种情况。
我们的初衷是使 o 尽可能多，所以只能填 o.o.o，这样可以使 o 最多且不相邻
不相邻的方法还有，可以填 .o.o. 或者 ..o..，但是这些方法的 o 都没有我们那个方法的 o 多，所以不行
我们的初衷是使 o 尽可能多

那么奇数个 ? 偶数个 ? 我们都知道怎么处理了。
但是如果 o 的数量不对怎么办？

一、少了
我们已经尽可能的加大 o 的数量，如果还是少了就说明无解，所以这种情况不存在

二、正好
直接输出处理过的即可

三、多了
如果多了，只需要在字符串的 o 里任挑几个删掉即可
然而，除了输入本来就有的 o 之外，其他的 o 都是被填充上去的，所有 o 都可能被删掉变成 .，所以所有位置都不确定是 o 还是 .，
直接还原到最初那个首要的填充之后，也就是 .o.o. 那个填充之前。
把那时的字符串输出即可，那时的字符串是没有填 o 的
为什么不能还原到输入的地方呢，因为有些地方是确定的，比方说 o?，这个 ? 只能填 .，不是贪不贪的问题了。我们首要的填充就是做的这个东西。
怎么实现还原呢，在那个位置备份一下字符串就可以了。

---

2025/4/14
P1312 Mayan 游戏
超级无敌大模拟，没啥好讲的，我就说一点细节吧

+-----------------------+
这道题主要是细节
一些函数
首先要用到深搜
不需要剪枝
然后要写
移动函数
下落函数
以及判空函数
消除函数
最长的是消除函数
但长不意味着难
都很简单
然后就是移动时
注意往左只能让左边为空
为什么呢
因为
不为空的话就意味着要交换
而往左交换还不如
左边的往右交换
这样子字典序还小一些
这就是这道题了
主要是代码长难维护
其他的就 AC 了额
+-----------------------+

---

2025/4/14
P2960 [USACO09OCT] Invasion of the Milkweed G
bfs
我不知道这题为什么要评黄，我倒是觉得挺橙的
这道题就是 bfs，不需要开 vis，因为走过的可以直接用 'M' 在地图上标记，走到的有 'M' 就直接不枚举了。每个点都记录一下步数，取最多的当答案，AC!!!

---

2025/4/21
P3378 【模板】堆
我们的高级数据结构二叉树之旅开始了
这道题就是堆，可以用 priority_queue 但是其实题目本意是让你手写。用树状数组维护，插入删除写好，然后不停的维护，修复，最后就 AC 了！！！

---

2025/4/22
P2168 [NOI2015] 荷马史诗
这道题是我见过第一道 Huffuman 解的题。
Huffuman 树就可以，要存深度，合的时候计算答案，合起来编码长度一起 + 1，所以答案的贡献直接取频率的和即可。然后就是开头要判断够不够合，不够就加 0，开个 long long 比较函数写一写，用上优先队列，就 AC 了。

---

2025/5/2
ABC403 D - Forbidden Difference
这道题我也没想到，是动态规划！！！
对于 d = 0 的情况，我们直接去重就可以，具体做法就是统计一共有多少个不同的数字，这些数字各取一个保留，剩下的直接删掉。
d != 0 的话，情况就复杂很多了，咱们直接 开挂（划掉）动态规划。
一个树可以被表示成 k * d + u 的形式，我们的 d 就是输入的那个，k 和 u 则是算出来的。可以看到，(k - 1) * d + u 和 k * d + u 是刚好差 d，于是我们就大循环枚举 u 小循环动态规划 k：
定义：设 dp[k][0] 表示当不保留 k * d + u 时我们最多能保留多少，dp[k][1] 表示保留 k * d + u 时我们最多能保留多少。
转移：dp[k][0] = max(dp[k - 1][0], dp[k - 1][1]);
不选 k 我随你的便，因为怎么选都不会不合法。
转移2：dp[k][1] = dp[k - 1][0] + cnt[k * d + u];
不能同时选 k 和 k - 1，因为他们差 d，不能同时保留。保留 k 就不能保留 k - 1，同时要加上 k 在数列里出现的次数。
初始化：dp[0][0] = 0, dp[0][1] = cnt[u]; 这个很显然
答案：n - max(dp[k - 1][0], dp[k - 1][1]); 保留最多的即可，注意题目问的是删多少个，所以要用 n 减。

做完以上这些，你就 AC 了！！

---

2025/5/4
ABC404 D - Goin' to the Zoo
这道题，别看他题目说一大堆，数据范围……
n <= 10
m <= 100
看到这里，我心中涌起一股邪恶的念头，这道题能用 dfs 吗……
考虑一个动物园能去几次，首先你可以不去，然后你可以去一次，或者去两次（也就是让动物园里的动物全部满足条件）。想一想，能去 3 次及以上吗？
事实上没这个必要，两次就可以让动物园里的动物全部满足条件，何必要去第三次呢？去了也是白去，让所有动物都被看 3 次？这个对答案没有贡献，而且不是说动物园给你随便去，要花钱，你多花一笔钱白去一次，只会给答案增加负担……
我们将输入数据整理成类似第 i 个动物园有哪些动物的形式， dfs 去每个动物园的次数，开个 cnt 数组，cnt[i] 表示第 i 个动物看了几次，一轮 dfs 完成时，就检查第 i 个动物被观赏的次数是否 < 2，<2 直接 return，否则更新答案，return.
时间复杂度：O(3^nnm)，不带常数有 59,049,000，< 64,000,000，可以通过！！！

---

2025/5/4
P1801 黑匣子
作为一道不想成为模板题的堆模板，这道题注定不平凡……
首先，肯定我们得开个堆，开几个堆呢？请看后面
我们要看第 i 小的数是几，就可以开两个堆，第一个堆存 1 ~ i - 1，第二个堆小根堆，存 i ~ m，我们查询时只需要将第 2 个堆的堆顶弹出来即可。
那插入呢？如果直接放第二个堆的话，如果比第一个堆中最大的元素都小怎么办，这时就应该放第一个堆（选读：请大家思考，这时第一个堆中就有超过 i - 1 个元素，与我们初衷不符，所以必须把最大的那个元素放小根堆里），否则就应该放第二个堆，否则第一个堆就太小了。
这时我们发现，插入元素必须快速查询第一个堆中最大的元素才能正常判断，故此，第一个堆是大根堆。将插入部分稍稍优化，我们放元素时优先放大根堆，再将大根堆中最大的元素弹出来进小根堆，要查询时就输出小根堆最小的元素，同时，由于 GET 后 i 会加一，大根堆的容量会随之加一，将小根堆中一个最小的元素放到大根堆里。这道题，AC！！！
这种处理方法，没想到吧，叫做双顶堆！！
所以这题不仅是堆的模板变形，还是双顶堆的模板……

作为一道不想成为模板题的堆模板，这道题注定不平凡……

---

2025/5/5
P1168 中位数
水题就是水啊……
不出所料，本来得双顶堆，现在倒好，借助来自 STL 黑暗神秘的洪荒之力
轻松水过好吧
vector × upper_bound
每次插入时就像插入排序一样用 upper_bound 插入，要取中位数就直接 a[(i - 1) / 2]。AC！！

---

2025/5/5
P1631 序列合并
简洁的题面，你绝对想不到的解法，It's my favorite!!!
首先看向 a 递增 b 也递增，于是我们只需要用 a[1] + b[i] 的形式放进队列，最小的输出，然后让最小的稍稍变大一点(a[2] + b[i])，以此类推，直到输出了 n 个为止！AC！

---

2025/5/5
P4053 [JSOI2007] 建筑抢修
贪心直到建（po）筑（chan）倒塌
贪 t2
先给 t2 排序，用结构体
然后遍历每一个可怜的建筑
看时间
能修就修，还可以功德 + 1
不能修就穿越时空
跟 now - q.count(q.top()) + 1 秒前的自己说，这个建筑是耗时最长的一个，别修了
然后回来继续修
最后输出你的功德，AC！

---

2025/5/11
P2085 最小函数值

这道题，数学+堆
我们用图形计算器稍稍模拟（用代数法稍稍证明）就可知，对于一个二次函数，a > 0 b > 0 c > 0 x > 0 的部分一定是单调递增的哈哈哈
于是直接套序列合并的那套
AC
不懂就去看序列合并那道题……

---

2025/5/18
ABC406 D - Garbage Removal
坑爹啊
这道题，是 ABC 比赛里 7 题中唯一一道时限有 2.5s 的题，这意味着程序可以进行 64000000 * 2.5 = 160,000,000 次循环，超过了 10 ^ 8 次方，在加上数据的s h u i，可以有 O(n ^ 2) 的复杂度！！！
So 直接暴力即可。

---

2025/5/26
P2278 [HNOI2003] 操作系统
这道题较为简单（虽然很蓝），我们可以将进程按照处理时间排序，然后枚举并进行判断：如果队中元素的结束时间比这个进程的开始时间要早，就直接把队中的这个进程结束了，否则开始抢夺 CPU，如果队中优先级最高的元素高于第 i 个进程的优先级，就将队中的那个元素弹出，再将第 i 个进程入队，否则，直接处理 i 后继续枚举下一个。这样这道题就 A 了，用到了优先队列。

---

2025/5/26
在常用的计算机中，有符号数采用补码表示。如果有一台采用反码表示的计算机，该如何实现 lowbit 函数？如果是原码表示呢？

这是一个好问题，我们都知道补码是反码加一，因此，如果采用反码表示，那就是 a & (-a + 1)。
原码表示比较复杂，我们设 num 为原始数：
00101100
-num 为
10101100
减一
10101011
很显然减一之后，末尾的 0 全都变成了一，而最后一次出现的 1 变成了 0，其他的都没变
与正数 num 与一下
00101000
（变成正数了）
除了符号位，我们将最后一个一与后面的部分与掉了，lowbit 正是求这一部分，与正数 num 相减即可。
100
归纳总结，原码的 lowbit 表示方法为 a - (a & (-a - 1))
补码表示 lowbit 还是最简单的，这也是补码的一个优势，果然现如今许多计算机都用补码！

---

2025/5/26
P1966 [NOIP 2013 提高组] 火柴排队
发扬你的人类智慧，在地球待了 46 亿年的经验告诉你，将相对大小相同的火柴怼到一起，∑(a[i]-b[i])² 最小，证明就不说了
将两队火柴排序（保留 id）后将 id 映射起来，因为作为 key，a-id 具有天然的有序性，按照 b-id 交换火柴，就可以恢复如初，变成最开始刚排完序的火柴的样子。
同样的，只需要按照 a-id 的指示交换 b 中的元素，就可以得到 ∑(a[i]-b[i])² 的值
那直接按照 a-id 排 b，存在映射表 c 中，a(key) 已经天然有序了然后要让 c 中的 b(value) 有序，求最小交换次数，这不就逆序对吗，直接树状数组 AC。

---

2025/5/26
P5677 [GZOI2017] 配对统计
散点树状数组的开端
先离散化，这很重要，不进行这个操作会让你的程序非常难受（程序：1e9 的数据你让我枚举，你礼貌吗），如果这影响了你对后续操作的理解，你就当作没有离散化（程序：……）
你把这些数字当作数轴上一个一个的点，那么一个数对应的好对，就是他左右两边，左边近就和左边是好对，右边近就和右边是好对，一样近就都是好对，排序后判断即可。
要把这些好对全放进树状数组吗？不需要。全放进树状数组里会增加查询的负担，只需要放右端点 < r 的即可，所以按右端点排序，把每个题目按照右端点排序，就可以通过逐步移动右端点来将左端点加入树状数组，通过 sum 来计算 >= l 的左端点数量，即可求得答案！

---

2025/5/26
P2161 [SHOI2009] 会场预约
STL 是个好东西
通过定义线段的比较方法 return a.r < b.l 可以实现很多意想不到的操作。
解释一下，我的代码中有详细注释，内容如下：

如果 A < B 为 true 说明 A 在 B 前
A > B 为 true 会被编译器自动编成 B < A 为 true
说明 B 在 A 前，A 在 B 后
如果冲突了
那就是 a.r > b.l 但 b.r > a.l
也就是 A < B 为 false
A > B 也为 false
A 不大于 B 也不小于 B
编译器就认为他们相等
std::set 可以查找元素
这就意味着使用 set 可以查找所有与当前安排冲突的安排

这基本上就把题目的主旨讲清了，用 set 维护线段，用这种特殊的比较方法，可以让冲突的线段被 set 认为是相等的（一个小小 bug），只需要用 find 函数就可以解决这道题！

---

2025/5/26
P3586 [POI 2015] LOG
紫紫紫紫题！！！！
叠馒头
看 B 站
很显然，剪掉超出 s 的到达了 c*s 就可以通过，否则 NIE。
那么开两个树状数组，一个维护种类数，一个维护数量和即可。

---

2025/6/1
P3870 [TJOI2009] 开关
在此之前的那个线段树模板，书上已经讲的很详细了，我一般不写模板的复盘日记，这道题在线段树的基础上加了一点点修改，不过内核没变。
只用改 pushdown 和 maketag
延迟标记只能 01 翻转，所以参数少一个 x，然后 lzy ^= 1;
w[i] 的处理上，因为每次开关一下，原先没开的灯就开了，开了的关了，所以 w[i] = (r - l + 1/*区间长度*/) - w[i];
pushdown 加一个 if (lzy)，这里也是问了 ai 才得出结论，不管 lzy 如何，你搞一次 w[i] 就会翻转一次，而 pushdown 出现在我们代码各处，如果不加一个判断执不执行的条件，w[i] 就会被多余地翻转很多次。这些之后就是模板了，AC。

---

2025/6/2
P1438 无聊的数列
啊啊啊书上说和线段树模板没有什么不同，结果亲手写了才知道，比书上的复杂很多……
先把模板写了，然后狂改
首先是 maketag，这次的 maketag 需要五个参数：u, l, r, st, gc。u, l, r 还是原来的意思，st gc 就是首项和公差，你要开两个延迟标记，一个存首项，一个存公差，这两个都要下传。w[u] 直接开一个自动求等差数列的和的函数，然后往里套就行了，w[u] 直接加等差数列的和。别问我怎么求和，上过小学的都知道
maketag 改了，pushdown 就必须改，我们的 pushdown 会把线段分成两半，左边直接套就行，右边还得算首项，l + gc * (m - l + 1)。然后根据 maketag 的五个参数自己补充就可以了。
最后一个受到影响的函数是 update，有交集的递归式按照参数写就可以，完全包含时 L 和 l 不一定一样，所以首项也不一样，还得算，     k + (L - l) * d。然后改了这么多终于能 A 了，注意 long long。

---

2025/6/2
P1253 扶苏的问题
要改很多。
双标记线段树。一个存加上的，一个存赋值的，这两个只能存在一个。剩下的重新维护一下就 A 了。具体方法可以仿照👆这道题。注意那个输入输出很坑，因为没有加速 T 了最后一个点，加速了就 A 了。

---

2025/6/15
P4513 小白逛公园
期末复习压力大，半个月调出一道题……
先想 pushup，然后就没有然后了……
首先最大子段和没有可加性，所以线段树就被直接淘汰掉了，要做就去学树形 dp 哈哈哈
但是如果硬要用线段树呢……
考虑用结构体存储线段（因为数据太多），l,r 为左右端点，sum 表示和，val 表示最大子段和。
在 pushup 汇总的时候，l、r、sum 都能轻松搞定，就是这个 val 有亿点难搞
前面就提到了，最大子段和这个玩意没有可加性，-1 1 和 1 -1 的最大子段和都是 1，合起来就是 2。但是 -1 1 和 -1 1 的最大子段和也都是 1，合并起来就是 1 耶……
那么对于这个非常毒瘤的子段和，他可以从三个地方来，第一个，从左孩子那儿来。
比方说：-1 1 0 2 3 和 -1 2 1 3 2。
第二个，从右孩子来，逻辑是一样的。
第三个，也就是  左孩子 的 右边   加   右孩子 的 左边。不觉得那么绕了把
举个栗子，-1 1 0 -1 1 2   +  2 1 3 -1 0 1
他的最大子段和是    1 2   +  2 1 3
把从左边开始的子段和称为左连续和，把从右边开始的字段叫做右连续和。那么这种情况下，最大子段和就是：
左孩子 的 右连续和     +      右孩子 的 左连续和
我知道你还是很绕，但是我只能这样了
欸，有提问

 	| 建议：
 	|
 	|  能不能把左连续和与右连续和封装成线段类型的成员？
 	|  可以更方便的计算与合并。
 	|
 	|  👍10000 👎0

非常有道理
于是我们的 Node belike:

struct Node {
    int l, r; //左右端点
    int sum, val; //sum 表示和，val 表示最大子段和
    int lv, rv; //左连续和以及右连续和
} w[2000005];

最大子段和就是我们前面讨论的三种情况中最大的一种，于是

void pushup(int u) {
    int Lnode = u * 2;
    int Rnode = u * 2 + 1;
    w[u].sum = w[Lnode].sum + w[Rnode].sum;
    w[u].val = max(max(w[Lnode].val, w[Rnode].val), w[Lnode].rv + w[Rnode].lv);
    w[u].lv = max(w[Lnode].lv, w[Lnode].sum + w[Rnode].lv);
    w[u].rv = max(w[Rnode].rv, w[Rnode].sum + w[Lnode].rv);
}

其他的非常简单
查询的 query 返回要返回一个线段，线段的计算与前面的 pushup 大差不差，但是不能直接用 pushup，自己写一个就可以
本题结束！

---

P1471 方差
多标记线段树数学化模板
平均数好维护，就是维护个区间和然后 / 长度就是了，关键是这个方差。
方差的公式：（设 a 为平均数）
S = ((a1 - a)^2 + (a2 - a)^2 + ... + (an - a)^2) / n
= (a1^2 - 2aa1 + a^2 + a2^2 - 2aa2 + a^2 ... + an^2 - 2aan + a^2) / n
= ((a1^2+a2^2+...+an^2) - 2a(a1 + a2 + ... + an) + na^2) / n
由于 nx = (a1+a2+...+an)，So
= ((a1^2+a2^2+...+an^2) - 2na^2 + na^2) / n
= (a1^2+a2^2+...+an^2) / n - 2a^2+a^2
= (a1^2+a2^2+...+an^2) / n - a ^ 2
假设 psum 为平方和

结论写大点
S = psum / n - a ^ 2
哈哈哈只要维护平方差与平方和即可！！
AC ！！

---

MarsOJ P1830【模板】矩阵加速（数列）
F A 求出来就可以
因为模数卡了半天，是 1e9+7 不是 1e7+7，这个容易打错
建议还是不要像题解那样，把 F 搞成 3x1，不能直接乘，建议 1x3

---

ABC415 C - Mixture
这道题很简单，看那个 n <= 18 你们就懂了（暴力出奇迹）
整体复杂度是 2 ^ 18 (* Test)。2 ^ 18 不是很稳定，可能会重复，但是这个 dfs 很适合记忆化，就记忆了。复杂度很不错。这里的参数本来是 pos 和 num，渐渐地发现 pos 没用，就删了，改了改，整理了一下，把一些数组都删了，换成了更好地表达。例如 vis 数组废除了，换成判断 num & (1 << i) 是否为 1 来代替。屡次优化之后终于卡过了 AC！

---

ABC415 D - Get Many Stikers
纯纯的贪心啊，不懂的话解释一下
首先明确一下：空瓶和满瓶本质上没有区别（
简单反证一下，认为有区别的朋友们，空瓶能换满瓶，满瓶能变空瓶，他们之间是可以相通的。除此之外他们没有什么区别
举个例子，虽然我换到的是满瓶，但是满瓶只能干一件事：变成空瓶。还不如大胆改题：
第 i 种交易方式指的是以 a[i] 个空瓶换 b[i] 个空瓶加一个贴纸
谁会那么神经空瓶换空瓶啊，没关系，更抽象一点：
第 i 种交易方式指的是以 a[i] 元换 b[i] 元加一个贴纸
如何使贴纸尽量多？
很明显啊，a[i] 元换 b[i] 元和一张贴纸，那么贴纸的价格就是 a[i] - b[i]。按照贴纸价格从小到大排序，然后遍历，优先买价格小的，然后一直往后枚举，越来越大，最后就是答案了。答案的计算这里不再赘述。

---

P11378 [GESP202412 七级] 燃烧
keng ti
tai keng le
我最开始说打一个 bfs 吧，结果 T 了。40pts
发现正解是 dfs
我就很奇怪，寻思怎么 bfs 还会比 dfs 慢呢？bfs  n^2 的复杂度，dfs 2^n 的复杂度，这怎么 bfs T 了，dfs 反倒能过呢？
结果发现人家的 dfs
开了记忆化！！！
开记忆化其实很简单
因为这题的 dfs 本来返回值就是 int
我果断写了记忆化
然后就过了
zhen keng
keng si wo la
如果这道题没掉陷阱我的模拟 gesp 分数能从 73 直线上升到 88
唉

---

MarsOJ P2107. 终曲（加强版）
思路跟普通版差不多，就是开个 lower_bound 即可。
变量名命名的问题不再赘述
这道题本来看了 classin 是要用二分+哈希去更新的
我寻思说啊？这么复杂？
结果根本不需要二分哈希
正常比大小比字典序
为什么呢
因为大部分时候你的两个字符串是不会相同的
100pts AC

---

GESP 七级 202403 第一题 交流问题
要讲这道题，首先得讲一个全新的算法：二分图。
什么意思呢？
很简单，其实就是一种特殊的图染色。
有一个图，你给他染两种颜色黑和白。现在我告诉你，两个相邻的节点不能染同色，然后你染出来的就是个二分图。
都说到这了，如何才能染呢？
很简单，直接用 dfs 遍历一遍就可以了

void dfs(int u, int f) { //u 表示当前节点，f 表示你要染什么色
    vis[u] = 1; //用 vis[i] 表示 i 有没有被访问过
    //f = 1 表示白，f = 0 表示黑
    //如果说我们需要存颜色的话，我们用 c 数组表示图的颜色
    c[u] = f; //给这个节点染色
    //如果我们需要存有多少个红，多少个蓝的话
    //orz
    if (f == 1) s1++; //如果是白, s1++
    else s2++; //否则 s2++
    for (int v : g[u]) { //翻译成人话就是，枚举所有相邻节点
        if (vis[v]) continue; //我们不能访问重复的节点
        dfs(v, !f);
        //第一个参数表示下一步到 v
        //第二个参数对 f 取反，如果 f 是白，那么下一个就是黑，反之亦然，解决了
        //相邻的元素不能同色的需求
    }
}

那么现在你大概已经懂了二分图的用法，回来看题，会发现我们只要对每个连通块染色，就可以得到答案。为什么呢？因为题中的 A 校学生和 B 校学生的朋友关系汇成了一张无向图。题目描述说只有异校学生才会交友，这个图中也只有异色节点才会相连。题目说要求无向图 B 校的规模，就是说求白点最多会有多少个（注释 orz 下面的部分）。我们只需要枚举每个连通块（因为有可能会出现图不连通的情况），对于每个连通块，假设头号节点是黑点，dfs 一遍，再假设他是白点，然后 dfs，记录下来。将最大值和最小值分别相加，就能得到人数总数的最大值最小值，也就是 B 校的规模。
AC.

---

GESP 七级 202403 第二题 俄罗斯方块
DFS。
用 dfs 洪水填充每个方块，存储 dfs 序（dfs 回溯时写空格）。易知每个相同形状的俄罗斯方块的 dfs 序也是相同的，反之亦然。所以开个 map，map[s] 表示 dfs 序为 s 有没有出现过，最后枚举计算有多少种输出即可。其实比第一题简单很多。

dfs 序，就是 dfs 枚举的顺序。比如我的 dfs 是先往上走了两轮，然后回溯了一次，在往右走了一轮。那么假设上下左右的编号分别为 1234，回溯是空格，那么我的 dfs 序就是：
11 4
每种俄罗斯方块的 dfs 序都是唯一的。

---

MarsOJ #P2297. 关押罪犯 NOIP2010 提高组 || 洛谷 P1525 [NOIP 2010 提高组] 关押罪犯
这道题作为 2010 年 S 组压轴题
肯定有他自己的难度……

其实解法很简单。
我们发现这个乌纱帽他想让冲突最大的事件尽可能小
所以我们要尽量把注意力放在冲突最大的事件上，我们发现冲突最大的事件他一定是被分配到单独的监狱，这样可以最大限度地减少冲突
我们使用拓展域并查集解决这个问题，为了防止冲突，我们将每对结仇的罪犯按照影响力从大到小排序，然后依次枚举，只要还能分离，我们就将这两个罪犯分到两个不同的监狱
怎么做呢？很简单，merge(u, v + n)，再 merge(u + n, v)，这样就能保证 u 和 v 不在一个集合里
如果发现 u 和 v 在一个集合里了，就说明冲突无法避免了，我们就停止枚举并输出当前的影响力即可
AC.

---

P13018 [GESP202506 七级] 调味平衡
没想到七级第二题竟然调这么久
这道题的思路其实很简单
DP
转移方程很好列，由于题目说每个食材的酸度和甜度都是确定的，并且一种食材要么选，要么不选。最后问你的是在酸度甜度相同的情况下，总和最大是多少。直接处理酸甜相同的情况显然不现实，我们可以退而求其次，将酸甜度之差作为重量，酸甜度之和作为价值。这道题就变成了，重量为 0 的情况下，价值最大是多少
我们开个 dp[i][j] 表示前 i 种食材，重量为 j 的情况下，价值最大是多少
转移方程：
dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - w[i]] + v[i])
而 w[i] = a[i] - b[i]
v[i] = a[i] + b[i]
在 j < w[i] 的情况下直接继承 i - 1
但是就有一个很严重的问题（这道题的难点）
那就是 w[i] 可能为负数
第一种情况：j - w[i] > j，由于 j 最大到 100000，而 w[i] 最小 -500，所以数组要开到 100505 才可以。
第二个影响，由于 w[i] 可以为负数，所以其实可能出现 j 为负数的情况，我们发现 w[i] 的和最小为 50000，所以我们把所有 j 都加上 50000，这样就能避免 j 为负数的情况
可得 dp[0][50000] = 0
其他 dp[0][j] = -inf
cout << dp[n][50000] 就可以了
（50000 指代的是原来的 0）
AC.

---

2025/10/4
10.2 T1
赛事爆 56pts 这也太……
正解其实跟我的思路差不多
（骗你的，差了非常多）
首先关键点：n <= 500
这也太爽了
我们疯狂开数组
先开一个 id[x][y] 给 (x, y) 这条边编号
再开一个 dot[id][op] 表示 id 边的 op 端点
然后开一个 deg[x] 表示 x 点的度数
然后开一个 vis[id] 表示 id 边有没有连接
然后就是直接开整
定义两个函数
一个 put, 一个 bfs
put 会把所有度数和 >= k 的边连接上（连接的方式就不用我多说了）并放入队列
bfs 从队列中取出边，删掉这个边的两个端点的所有边（vis 置 0，deg 减 1），然后把这两个端点的所有边重新 put 一遍
最后输出答案
AC

---

2025/10/8
10.6 T1
这道题一点也不简单（
太绕了
代码都是 AI 帮调
来讲讲反复看了题解 20 遍之后的我的思路
首先我们要知道题目问的是什么
题目就很绕
https://www.luogu.com.cn/problem/T676142
就是有一个 n, m 和一个数列 a
问你对于每一个 k，有多少个从 a 中选取的集合 S
满足有一个从 S 中选取的 T，T 与 S 的长度相差 k，并且 T 元素之和 > m 
是不是很绕
组合数+dp
好的现在应该你们已经红温了
S 组 T1 这么难？!（没关系作为模拟肯定是比真实难一点）
首先 a 的顺序与答案无关，那我们就手快给 a 从大到小排个序
假设 T 固定
那么 T 的最后一个元素的位置（设为 i）后面到 n 的部分只需要选 k 个元素就行了，直接组合
加起来就行
T 用动态规划背包求，dp[i][j] 表示前 i 个选一些元素之后和 = j 的方案数，dp 出来了易求得 T 的方案数
对于每个 k，两个乘起来即可

---

ABC429 D - On AtCoder Conference
题目大概就是说，有 n 个人，每个人都站在岸边并且都有自己的位置 x（就是离木屋的距离）。定义 x[i] 为高桥从 i 点出发时，遇见至少 C 个人时遇到了 x[i] ge 人。求出 x[i] 的和。

这道题的难点在于池塘的周长 m 非常大，可以达到 1e12。仔细阅读题目，发现一个非常神秘的条件：C <= n。这也就意味着，无论如何，高桥都不会走到超过一圈。于是考虑二倍长度的拆环成链，把池塘拆成链。

对于 m 的极大值，我们发现其实大部分时候，池塘上的很多点都没有人，高桥经过这些点就不会遇到人，那么这些点除了作为起点外就不会对 x[i] 有影响，也就是说，完全可以忽略。我们通过 map 存储输入数据，然后将 map 的数据拆成链存进一个 vector 里面，双指针 vector 即可。

AC.

---

2025/11/2
ABC430 C - Truck Driver
通过双指针来解（其实是三指针）
考虑对于每个 l，将 r 可能出现在的区间用 [r1, r2) 表示
那么 r1 就是第一个满足 'a' 的个数 >= a 的位置
r2 是第一个满足 'b' 的个数 >= b 的位置
如果 l 增大一个
第一个字符就会消失
那 'a' 的数量与 'b' 的数量不会增加
故 r1 和 r2 的位置不会提前
利用这个性质进行双指针
每次当 l 增大时
维护 r1 和 r2

for (int l = 1; l <= n; l++) {
    while (r1 <= n && ca1 < a) { //增加直到 >= a
        ca1 += s[++r1] == 'a';
    }
    if (r1 > n) break; //如果超出了范围，说明后面一共都没有 a 个 'a'，直接结束
    while (r2 <= n && ca2 < a) { //先增加到 >= a，否则可能不满足条件
        ca2 += s[++r2] == 'a'; //为了不与第一个指针产生干预，换一个变量
        cb += s[r2] == 'b'; //维护 b 的数量，后面需要用
    }
    while (r2 <= n && cb < b) { //增加直到 >= b
        ca2 += s[++r2] == 'a';
        cb += s[r2] == 'b';
    }
    ans += r2 - r1; //位于 [r1, r2) 内的位置都可以与 l 形成合法数对，半区间大小 = r - l
    //下一轮循环的 l 会变大，还原
    ca1 -= s[l] == 'a';
    ca2 -= s[l] == 'a';
    cb -= s[l] == 'b';
}

最后输出答案即可。

date：
赛时尝试用二分查找，但是由于一些性质，没有解决（样例 1 输出 0 ）
赛后开始用双指针
第一次：在处理 r2 时，没有单独维护 r2 使其合法，而是简单的 r2 = r1
第二次：ca2 没有减
第三次：r1 用了 cb
第四次：不开 long long 见祖宗
第五次：AC.

---

2025/11/2
D - Neighbor Distance
通过 map 来维护一下每个人的位置与离邻居最短的距离，每次一个新人来时，更新他左右两个人与他自己即可。

for (int i = 1; i <= n; i++) {
    int x;
    cin >> x;
    auto r = a.upper_bound(x); //获得他的右边
    auto l = prev(r); //获得他的左边
    int resl = x - l->first; //求出与左边的距离
    if (!l->second || l->second > resl) { //如果左边暂时没有邻居或者邻居距离没有这个长
        ans += resl - l->second; //x 就是他最短邻居
        l->second = resl; //更新
    }
    if (r != a.end()) { //先判断右边存不存在
        int resr = r->first - x; //求出与左边的距离
        if (r->second > resr) { //同上
            ans += resr - r->second;
            r->second = resr;
        }
        resl = min(resl, resr); //顺便计算出 x 离哪一个更近
    }
    ans += resl; //更新答案
    a[x] = resl; //放入 x 
    cout << ans << '\n';
}

---

2025/11/9
P3375 【模板】KMP
KMP 思路简单讲一讲吧
首先是 nxt 数组
考虑如何求 nxt[i]
最理想情况：nxt[i - 1] 的最长前后缀往后一个与 s[i] 相同
意味着 nxt[i] = nxt[i - 1] + 1
第二种情况
要跳一次
nxt[nxt[i - 1]] 往后一个与 s[i] 相同
nxt[nxt[nxt[i - 1]]] 往后一个与 s[i] 相同
nxt[nxt[nxt[nxt[i - 1]]]] 往后一个与 s[i] 相同
……
给一个 j 表示 nxt[i - 1]
while, 不符合条件就跳
nxt 就求出来了

匹配也很简单
每次匹配串会往后移动一点
意味着 j = nxt[j]
不行就再 j = nxt[nxt[j]]
如此反复
直到匹配为止
写法和 nxt 的求法极其相似
可以说是几乎完全相同

---

2025/11/9
CF126B Password
发现一个事情
就是这个中间的字符串肯定是公共前后缀
不妨直接 nxt 一下 
nxt 到最大
然后开始
中间的这个串从 2 枚举
如果说这个 nxt[i] == nxt[n] 说明这个串最右边那个后缀一定与 nxt[n] 相同
输出答案即可

---

2025/11/16
C - Candy Tribulation
首先观察到一个性质：
假设有一个小盆友 i，x 是小糖果重量，y 是大的
a[i] 是小盆友想要吃的糖果数，w 是重量
设有 c1 个小糖果，c2 个大糖果
那么：
c1 * x + c2 * y = w
c1 + c2 = a[i]
如果说还有一个小盆友 j
d1 * x + d2 * y = w
d1 + d2 = a[j]
尝试将两式联立起来：
c1 = a[i] - c2
d1 = a[j] - d2
(a[i] - c2) * x + c2 * y = (a[j] - d2) * x + d2 * y
a[i]*x - c2*x + c2*y = a[j]*x - d2*x + d2*y
(a[i] - a[j]) * x = (d2 - c2)(y - x)
d2 - c2 = (a[i] - a[j]) * x / (y - x)
这样就求出两个人大糖果的差了
a[i] a[j] x y 都已知，所以只需要稍稍带入，理论上就能求出所有人的大糖果数量
（对，没想到吧，只要满足上面那个方程式，就能满足要求）
（所以小孩子们的糖果数量，只要差不变，就可以随便变动，可以自己试试）
最后剩下的就是后期了
我们令 j = 1
原式变为 d2 - c2 = (a[i] - a[1]) * x / (y - x)
为了确保 a[i] - a[1] 为正
将 a 从大到小排序
这样就可以求出所有人大糖果数量与第一个人的差
令他为 b[]
b[i] 表示的就是 i 所拥有的大糖果数量与第一个人的差
最后一点，也是我课上没搞懂的地方
就是求出第一个人的糖果数量
但是其实这个人的糖果数量（设为 v）是没啥限制的（因为差不变就可以嘛）
只有一个限制：
每个人的糖果数量不得大于 a[i]
这句话很好理解吧，题目里的
每个人的最大糖果数量是 a[i]
比第一个人多的糖果数量 b[i]
也就是说，第一个人的糖果数量不能超过 a[i] - b[i]
否则第 i 个人就会拥有多余 a[i] 个糖果，不满足要求
也就是说，v <= a[i] - b[i]
每个人的 a[i] - b[i] 都不同
第一个人的大糖果数量必须全部满足以确保所有人都合法
所以就可以取 min 啦
最后每个人的糖果数量就是 v 加 b[i]（除了第一个人自己是 v）
累加起来输出即可！
AC！（确实有点难吧……）

---

2025/11/16
E - Clamp
对于每种询问，有两种情况
第一种（也是坑）：l > r
题目没说 l 不能 > r
当然这种情况也好做
想想公式：max(l, min(r, a[i]))
min(r, a[i]) 一定 <= r（否则为什么不取 r 呢）
l > r
所以 max(l, min(r, a[i])) 的结果一定是 l！
那么这个询问的结果就统统是 l * n
第二种：l <= r
答案由三部分组成
1. x < l，这时会给答案带来 l 的贡献
2. x >= l && x <= r 带来 x 的贡献
3. x > r 带来 r 的贡献
将每种数简单粗暴的开个桶分类讨论即可
具体做法？
开两个树状数组，一个 t1[] 存分布情况（统计数字种类），一个 t2[] 存存在的数字之和
lowbit update query seq 稍微改一下参数列表，使得能兼容两个数组
那么三部分的贡献就是
1. seq(t1, 0, l - 1) * l
2. seq(t2, l, r)
3. seq(t1, r + 1, MAX) * r
树状数组在下标为 0 时会产生死循环，所以需要加一个坐标偏移
那么三个贡献也要变：
1. seq(t1, 0, l) * l
2. seq(t2, l, r) - seq(t1, l, r)
3. seq(t1, r + 2, MAX + 1) * r
AC.

---

2025/11/24
D - 183183
（并不知题名的典故）
题目的定义是 f(a, b) = "a" + "b"，即把两个数连起来
比方说 f(1831, 83) = 183183（题目典故）
很明显，假如定义一个 get(x) = x 的位数的话，那么
f(a, b) = a * 10 ^ get(b) + b
现在这个数要被 m 整除
(a * 10 ^ get(b) + b) % m = 0
也就是
(a * 10 ^ get(b) % m + b) % m = 0
a * 10 ^ get(b) % m = -b % m
如果 b 已知，能求出 a 可能的所有情况的话，就可以满足条件了
所以枚举 b
关键是怎么求出 a 有多少种情况
看起来很难，其实有一个突破口
作为一个数，位数并不会太多，最多也就 10 位
开一个 map 数组 cnt
cnt[j] 表示当 -b % m 为 j 时，a 有哪些情况
由于每种位数要分类讨论，所以要开二维：cnt[k][j]
输入完 a 后，对于每个 k，可以通过 a * 10 ^ get(b) % m = -b % m 的性质，对 cnt[k][a * 10 ^ get(b) % m] 自增。
最后枚举每个 b，ans += cnt[get(b)][-b % m/*代码中应该谢 (-b % m + m) % m*/]; 输出 ans 即可。
AC。

---

2025/11/30
C - Flapping Takahashi
相信大家都玩过不死鸟的游戏
就是那个小鸟可以上飞下飞但不能撞到地与水管的那个
本题与那个很像
限定不同特定时间时的不同高度区间，问能不能达成所有限定条件
假设高桥最开始的位置位于 h0
则 h0 = H
如果高桥飞行 t 时后的高度为 h1，则
h0 - t <= h1 <= h0 + t
考虑到不能触地，并且简单推算易得往 1e9 以上的高度飞行是没有意义的
所以
max(h0 - t, 1) <= h1 <= min(h0 + t, 1e9)
令 a = max(h0 - t, 1)，b = min(h0 + t, 1e9)
则 t 时的飞行高度可能区间为
a <= h1 <= b
而众所周知，我们的题目也有条件
l1 <= h1 <= u1
故此，h1 实际上合法的区间是 [a, b] 和 [l1, u1] 的交集
如果没有交集，就说明不可能合法，直接输出 No 并结束
接下来怎么办呢
先把交集更新到 a 和 b 里，然后再继续循环即可
这样程序就可以自己推出下一轮 t2 时我们的飞行高度在合法的情况下可能为多少
AC.
for (int i = 1; i <= n; i++) { 
    int c = t[i] - t[i - 1]; //处理飞行时间
    a = max(a - c, 1ll); //飞行的最低可能高度
    b = min(b + c, MAX); //飞行的最高可能高度
    a = max(a, l[i]); 
    b = min(b, u[i]); //求与目标的交集，顺便保存到 a 和 b 里
    if (a > b) return 0; //如果无交集，return false
}
return 1; //全部合法，return true
这里很容易忘记取交集，直接用目标区间作为可能区间去算（比如我）
但是目标区间与可能区间可能并不是包含关系，也就是说，如果直接用目标区间的话，很容易出现“看起来能够达到，实际上达不到”的情况
在此举个例子
t = 1
n = 2, h = 1
2 3 13
3 10 13
如果像我以前那样直接用目标算的话
程序会认为可以完成 2 目标
而 2 目标又和 3 目标有交集
程序觉得 2 目标可以完成，就是说高桥可以到达 2 目标的任何地方，于是程序觉得直接选择交集的位置往前冲就行了
所以程序认为可以完成 3 目标
但事实上，高桥只能到达 2 目标的最底下，并不能到达 2 目标的任何位置，而 3 目标的底部提高了很多，这就导致虽然完成了 2，但是没办法完成 3
所以其实是无法完成 3 的

---

D - Clouds
比较简单
二维的前缀和与差分
首先要快速计算出矩阵
差分即可
计算出来后可以统计一下有哪些格子本来就无覆盖，记为 cnt
接下来就是看有哪些格子会在对应地毯腾出来时无覆盖
有脑可知当且仅当格子覆盖数正好为 1 时满足上面条件
将不符合条件的（即覆盖数 >= 2 的）改成 0
然后再一次前缀和，记为 sum
枚举每片毯子，将毯子上的和计算出来，这就是“对应地毯腾出来时无覆盖”的格子，将这个加上本来就有的 cnt，即可得到答案，输出即可。
AC.

---

P4551 最长异或路径
简单题
先通过 dfs 计算出给出的树每个节点到根节点（无根树，令根为 1）的异或和，再以此建 01-trie，建好之后贪心即可。
枚举每个节点，由于要让这个异或和的异或值最大，所以往下走，如果有可以贡献的方案就优先选用（就是比方说原来是 1，那我就优先走 0），计算答案即可。
AC

---

2025/12/4
P1395 会议
树的重心
树的重心的定义是重量最小的节点
一个节点的重量定义为删除此节点和所有与其有关联的边后剩下连通块的最大大小
故此，去掉树的重心后的连通块大小一定最小
一棵树可能有至多 2 个重心（如果有两个，则其相邻，很容易想到）
可以证明，树的重心的重量不超过树的节点总数的一半

《可以证明》
+--- - - - - - ->
| 考虑反证法，令节点 v 为重心，并且 v 的重量 k > n / 2
| 去掉 v 之后的连通块可以大致分为两类。一是 v 的所有子树，二是其余部分（也就是 v 的上面带上根节点的
| 其他子树的大连通块）。
| 再计算一下 v 的一个相邻父节点 u 的重量
| 如果删除 u，那么剩下的是 u 的子树（包括 v）与一个大连通块
| 大连通块的大小是 k - 1
| 子树大小不可能超过一半
| 这也就意味着，u 的重量 < v 的重量
| 与 v 是重心矛盾，得证
+--- - - - - - ->

树还有一个性质，就是所有节点到树的距离和最短
因为这个和连通块有很大关系（连通块一定是树，那么在节点有限的情况下，高度也有限）
所以也非常容易证明
大家自己证证看吧，我这里就不《可以证明》了
好的那么我们就可以用这个性质 2 解这道题
通过写一个 dfs 来维护每个节点的重量，最小值即为重心
再写一个 dfs 计算距离和
AC.

---

2025/12/7
P3128 [USACO15DEC] Max Flow P
树上差分
网上教的很详细，我只写点差分，想学边差分自行小鲸鱼（
差分相信大家都很熟悉了
记录一些特殊点的偏移量，然后一前缀和就好
树上也是一样
先说树上前缀和是什么吧
树上的前缀和，本质就是从叶节点一直往上累加得到的
代码就是字面意思
从叶节点一直往上累加

void get(int u, int fa) {
    for (int v : g[u]) {
        if (v == fa) continue;
        get(v, u);
        a[u] += a[v];
    }
}

短的要命
核心就一句：a[u] += a[v];
差分的话
请看黑板：https://wbd.ms/share/v2/aHR0cHM6Ly93aGl0ZWJvYXJkLm1pY3Jvc29mdC5jb20vYXBpL3YxLjAvd2hpdGVib2FyZHMvcmVkZWVtLzcyNjE0N2YxZjRiMzQyYzE5NzVlNmQ2OTQ5NDE4YjJiX0JCQTcxNzYyLTEyRTAtNDJFMS1CMzI0LTVCMTMxRjQyNEUzRF9mYzU2ZDgzZC0zNTRlLTQ3MjktOGYzMC0wM2Y0YmZkMTVkYWM=
AC.

---

2025/12/7
P3258 [JLOI2014] 松鼠的新家
树上差分
跟上文几乎一样
连代码都是复制粘贴然后改了一点的（嘘
就讲一下改动吧
除了输入外，这道题需要给每两个节点之间进行自增操作，所以要挨个枚举，由于线段之间重叠的部分会重复加，并且题目说的最后一战小熊不拿糖，所以 a[2] ~ a[n] 的区域要 -1
我搞成 2 ~ n 了，10 分调了好久，最后考古历史帖子（也不算古，就在今年 8 月暑假），被一个神帖猛然点醒，后 AC 之。
